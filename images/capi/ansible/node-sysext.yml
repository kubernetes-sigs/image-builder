# Copyright 2026 The Kubernetes Authors.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
---
- name: Provision sysext-capable node base
  hosts: all
  become: true
  vars:
    node_custom_roles_pre: ""
    node_custom_roles_post: ""
    custom_role_names: ""
    sysprep_remove_swapfiles: false
    system: linux
    arch_map:
      x86_64: amd64
      amd64: amd64
      aarch64: arm64
      arm64: arm64
      ppc64le: ppc64le
    arch_uname: "{{ ansible_facts['architecture'] }}"
    arch: "{{ arch_map.get(arch_uname, 'unsupported') }}"

  tasks:
    - name: Include pre-node custom roles
      ansible.builtin.include_role:
        name: "{{ role }}"
      loop: "{{ node_custom_roles_pre.split() }}"
      loop_control:
        loop_var: role
      when: node_custom_roles_pre != ""
    - name: Include systemd sysext role
      ansible.builtin.include_role:
        name: systemd_sysext
    - name: Include post-node custom roles
      ansible.builtin.include_role:
        name: "{{ role }}"
      loop: "{{ custom_role_names.split() + node_custom_roles_post.split() }}"
      loop_control:
        loop_var: role
      when: custom_role_names != "" or node_custom_roles_post != ""
    - name: Include sysprep role
      ansible.builtin.include_role:
        name: sysprep
    - name: Include post-sysprep custom roles
      ansible.builtin.include_role:
        name: "{{ role }}"
      loop: "{{ node_custom_roles_post_sysprep.split() }}"
      loop_control:
        loop_var: role
      when: node_custom_roles_post_sysprep != ""

  environment:
    http_proxy: "{{ http_proxy | default('') }}"
    https_proxy: "{{ https_proxy | default('') }}"
    no_proxy: "{{ no_proxy | default('') }}"
    HTTP_PROXY: "{{ http_proxy | default('') }}"
    HTTPS_PROXY: "{{ https_proxy | default('') }}"
    NO_PROXY: "{{ no_proxy | default('') }}"
    PYTHONPATH: "{{ python_path }}"
